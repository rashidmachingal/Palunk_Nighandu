<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Malayalam meaning of something</title>
    <!-- custom css  -->
    <link rel="stylesheet" href="/css/result.css">
    <!-- custom js -->
    <script src="/js/search/search.js" defer ></script>
    <script src="/js/utils/utils.js" defer ></script>
    <script src="/js/word/add-new-meaning.js" defer></script>
    <%- include('../partials/header') %>
</head>
<body>
    <!-- navbar -->
     <%- include('../partials/navbar') %>
    <!-- navbar -->

    <div class="w100 item-center" >
        <div class="main-box" >
             <!-- search  -->
             <%- include('../partials/search') %>
             <!-- search  -->

             <%# render result list if word found start %>
             <% if (status === true) { %>
               <%- include('../partials/result') %>
             <% } %>
             <%# render result list if word found end %>

             <%# render add new word btn if word found start %>
             <% if(status === true){ %>
                <div class="result-add w100 mt-1" >
                 <button id="new_meaning_btn" >പുതിയ അർഥം ചേർക്കുക</button>
                </div>
                <div id="add_new_meaning" style="display: none;" ></div>
             <% } %>
             <%# render add new word btn if word found end %>

             <%#  render error message if word not found start %>
             <% if (status === false) { %>
                <%- include('../partials/notfound') %>
             <% } %>
             <%#  render error message if word not found end %>
        </div>
     </div>

      <%#  render if add new word button clicked start %>
      <%- include('../popups/add-new-meaning') %>
      <%#  render if add new word button clicked end %>

    <!-- footer  -->
     <%- include('../partials/footer') %>
    <!-- footer  -->
</body>
</html>